const Msg = ({name}) => {
//*   console.log(props) çıktısı object formatında;

//! desctructing,
// const {name} = props


  return (
    <div>
      <h3>Hello {name}</h3>
    </div>
  );
};

export default Msg;
